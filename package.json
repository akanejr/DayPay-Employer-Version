{
  "name": "daypay",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/prepare-env.mjs && vite",
    "build": "node scripts/prepare-env.mjs && vite build",
    "build:site": "node scripts/prepare-env.mjs && vite build --outDir site --emptyOutDir",
    "lint": "oxlint",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.109.0",
    "jspdf": "^4.2.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "oxlint": "^1.75.0",
    "vite": "^8.2.0"
  },
  "description": "DayPay \u2014 Know what your work is worth. Daily wage tracker with leave management, payslip sharing and cloud sync.",
  "author": "Akaninyene",
  "license": "UNLICENSED"
}
